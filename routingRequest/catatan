routing adalah istilah dalam menentukan respons server berdasarkan path dan url yang diminta oleh client
pastikan di package.json sudah menambahkan
 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  -- npm run start dari "scripts":{"start":"node server.js"}
  -- ke cmd (windows+r=cmd enter)
  
  curl -X POST http://localhost:5000
  curl -X DELETE http://localhost:5000

 curl -X GET http://localhost:5000/about 
 curl -X POST -H "Content-Type: application/json" http://localhost:5000/about -d "{\"name\": \"Hanbin Kim\"}"
curl -X PUT http://localhost:5000/about
curl -X DELETE http://localhost:5000/about